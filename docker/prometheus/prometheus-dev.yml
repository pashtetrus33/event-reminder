# Used by compose.yml (local dev — app runs on host)
global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: holiday-notificator
    metrics_path: /actuator/prometheus
    static_configs:
      - targets:
          - host.docker.internal:8085
