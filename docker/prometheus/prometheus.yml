# Used by docker-compose.yml (full deployment with app container)
global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: event-reminder
    metrics_path: /actuator/prometheus
    static_configs:
      - targets:
          - app:8085
